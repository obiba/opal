<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:tx="http://www.springframework.org/schema/tx" xmlns:batch="http://www.springframework.org/schema/batch"
  xsi:schemaLocation="
  http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd 
  http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd 
  http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-2.0.xsd">

  <batch:job id="csv-import" parent="abstractImportJob" restartable="false">
    <batch:step id="importCsv" parent="abstractDatasetImportStep">
      <batch:tasklet>
        <batch:chunk reader="csvDatasetReader" />
      </batch:tasklet>
    </batch:step>
  </batch:job>

  <bean id="datasourceService" parent="abstractDatasourceService" scope="step">
    <property name="datasourceName" value="#{jobParameters[input.datasource.name]}"/>
    <property name="entityType" value="PARTICIPANT" />
  </bean>

  <bean id="csvDatasetReader" class="org.springframework.batch.item.file.FlatFileItemReader" scope="step">
    <property name="resource" value="#{jobParameters[input.file.name]}" />
    <property name="linesToSkip" value="1" />
    <property name="skippedLinesCallback" ref="datasetMapper" />
    <property name="lineMapper">
      <bean class="org.springframework.batch.item.file.mapping.DefaultLineMapper">
        <property name="fieldSetMapper" ref="datasetMapper" />
        <property name="lineTokenizer">
          <bean class="org.springframework.batch.item.file.transform.DelimitedLineTokenizer" />
        </property>
      </bean>
    </property>
    <property name="recordSeparatorPolicy">
      <bean class="org.springframework.batch.item.file.separator.DefaultRecordSeparatorPolicy" />
    </property>
  </bean>

  <bean id="datasetMapper" class="org.obiba.opal.datasource.reader.DatasetFieldSetMapper" scope="step">
    <property name="catalogueName" value="#{jobParameters[input.catalogue.name]}" />
    <property name="extractionDate" value="#{jobParameters[input.extraction.date]}" />
    <property name="datasourceService" ref="datasourceService"/>
    <property name="lineTokenizer">
      <bean class="org.springframework.batch.item.file.transform.DelimitedLineTokenizer" />
    </property>
  </bean>

</beans>
