<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd">

  <bean id="xaDataSource" class="org.enhydra.jdbc.pool.StandardXAPoolDataSource" destroy-method="shutdown">
    <property name="dataSource">
      <bean class="org.enhydra.jdbc.standard.StandardXADataSource" destroy-method="shutdown">
      
        <property name="driverName" value="${org.obiba.opal.datasource.key.driver}" />
        <property name="url" value="${org.obiba.opal.datasource.key.url}" />
               
        <!-- Bind this XADataSource to the JTA TransactionManager -->
        <property name="transactionManager" ref="jotm" />
      </bean>
    </property>
    
    <property name="user" value="${org.obiba.opal.datasource.key.username}" />
    <property name="password" value="${org.obiba.opal.datasource.key.password}" />
    
    <property name="maxSize" value="10"/>
    <property name="minSize" value="3"/>

  </bean>

  <bean id="hibernateDbConfig" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
    <property name="properties">
      <props>
        <prop key="hibernate.dialect">${org.obiba.opal.datasource.key.dialect}</prop>
      </props>
    </property>
  </bean>

</beans>